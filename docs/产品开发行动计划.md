# MemoryMusic 产品开发行动计划

> 文档版本：V0.8<br>
> 制定日期：2026-07-15<br>
> 最近更新：2026-07-19（M4 完成，进入 M5 数据安全与自用收口）<br>
> 适用阶段：个人自用 MVP<br>
> 关联文档：[产品定位报告](产品定位报告.md)、[搜索设计与忘歌测试集](搜索设计与忘歌测试集.md)

## 1. 计划目标

MemoryMusic 当前目标不是建设完整音乐播放器，而是尽快验证以下核心闭环：

> 收录一首歌曲 → 低成本记录个人线索 → 用任意记忆点找回歌曲 → 调用网易云音乐播放。

本行动计划以个人自用 MVP 为范围，优先交付可验证的纵向功能，不提前建设社区、推荐算法、多设备同步或完整流媒体播放能力。

### 1.1 MVP 达成标准

当以下条件同时满足时，视为 MVP 核心能力成立：

- 能手工添加或从网易云导入至少 100 首歌曲；
- 能为歌曲添加多个标签、多条感悟、事件和自定义别名；
- 能统一搜索歌名、歌手、专辑及全部个人字段；
- 搜索结果能说明命中了哪些字段；
- 能从搜索结果唤起网易云音乐或可靠地打开歌曲页面；
- 能备份并恢复个人数据库，且个人数据不会因平台歌曲失效而丢失；
- 20 个真实“忘歌任务”中，至少 80% 能在 10 秒内找回目标；
- 主分支持续通过 Lint、格式检查、类型检查、测试和生产构建。

## 2. 当前开发基线

### 2.1 已完成

- [x] 完成产品定位、竞品、技术路线与未来规划；
- [x] 创建 GitHub 仓库并建立 `main` 分支；
- [x] 创建 `MemoryMusic` Codex Cloud 环境；
- [x] 建立 Electron + Vue 3 + TypeScript + pnpm 桌面脚手架；
- [x] 建立安全隔离的 preload API 与主进程/渲染进程边界；
- [x] 配置 ESLint、Prettier、TypeScript、electron-vite 和 electron-builder；
- [x] 本地通过依赖安装、Lint、格式检查、类型检查、构建与启动冒烟测试；
- [x] 脚手架已推送到 `origin/main`，基线提交为 `3917f3f`；
- [x] Cloud 数据层任务已审查、应用并完成本地加固；
- [x] 建立 SQLite v1 迁移、Repository、Electron 主进程生命周期和自动化测试；
- [x] M0 数据层通过文件重启、重复迁移、关系约束和删除语义验收；
- [x] 建立稳定的个人资料库 IPC/DTO、SQLite v2 迁移和主进程输入校验；
- [x] 完成歌曲、标签、感悟、事件、别名、错记歌词和其他线索的 CRUD 界面；
- [x] M1 通过共享标签、事件多歌曲关联、失败保留输入和成品重启持久化验收；
- [x] 建立 SQLite v3 扁平搜索文档、FTS5 trigram 索引和短词包含匹配；
- [x] 完成字段级命中解释、稳定排序、无结果反馈和搜索索引重建；
- [x] M2 的 25 条场景化忘歌查询全部命中预期首位，成品通过 v3/FTS 重启验收。
- [x] 建立独立的网易云协议唤起、网页兜底与 Windows SMTC 适配能力；
- [x] 搜索结果可一键发起播放，失败保留查询和个人资料并提供网页兜底；
- [x] M3 通过 Windows 11、网易云 3.1.36、真实歌曲、真实 SMTC 和隔离成品端到端验收。
- [x] 建立 SQLite v4 同步状态、平台无关 `NeteaseDataAdapter` 与 125 首 Mock 幂等导入基线；
- [x] 完成全局快捷键、SMTC 快速记录小窗、单标签/一句话保存和待整理箱，并通过隔离成品耗时验收。

### 2.2 已整合的 Cloud 结果

当前 Cloud 任务：

```text
task_e_6a574df3ddb88328a2870e7fb38aeab5
Implement MemoryMusic data layer with tests
```

该任务产生 5 个文件、约 `+415/-3` 的原始改动。任务 diff 已先行审查并应用到 `feature/music-data-layer`，随后补齐锁文件、迁移版本校验、真实文件重启、平台映射删除、Electron 生命周期和 native ABI 测试运行方式，最终通过本地验收后进入产品基线。

### 2.3 当前主要缺口

- 尚未接入网易云官方开放平台或收藏同步；
- 尚无备份、恢复和导出能力；
- 尚未积累真实自用忘歌记录和连续使用数据。
- `orpheus://` 是客户端版本相关能力，网易云升级后仍需按验证清单回归；

## 3. 开发原则

### 3.1 优先完成纵向闭环

每个阶段都应交付可操作、可验证的用户路径。数据表、接口或页面单独存在不算完成，必须至少能被下一层调用并通过自动化或人工验收。

### 3.2 本地优先

- 标签、感悟、事件等个人数据默认只保存在本地；
- 数据库应支持备份、恢复和开放格式导出；
- 网易云数据只作为外部内容引用，不拥有个人数据的生命周期；
- 平台歌曲不可见或被删除时，只更新状态，不级联删除个人记录。

### 3.3 适配器隔离

- 数据同步通过 `NeteaseDataAdapter` 等接口与领域层隔离；
- 播放通过 `PlaybackAdapter` 与搜索、标注逻辑隔离；
- Cloud 测试使用 Mock 适配器；
- Windows 专属能力必须在本地真机验收。

### 3.4 不碰版权与账号红线

- 不破解付费、版权、地域或 DRM 限制；
- 不把第三方非公开接口作为唯一正式依赖；
- 不在 Git、日志、数据库明文或前端代码中保存私钥；
- 优先评估网易云官方开放平台及官方工具。

### 3.5 每次只解决一个主要风险

开发顺序依次验证：

1. 数据能否可靠保存；
2. 个人线索是否容易录入；
3. 搜索是否真的提高找回率；
4. 播放跳转是否稳定；
5. 网易云同步是否值得维护。

## 4. 阶段路线图

时间按“有效开发周”估算，可根据个人投入调整；阶段验收未通过时，不进入依赖它的下一阶段。

| 阶段 | 建议周期 | 核心结果 | 主要验收 |
| --- | --- | --- | --- |
| M0：整合工程基线（已完成） | 1—2 天 | Cloud 数据层进入可验证分支 | 全部检查通过，可重复创建数据库 |
| M1：个人资料 CRUD（已完成） | 第 1 周 | 能手工添加歌曲并记录标签、感悟、事件 | 数据重启后存在，关系操作正确 |
| M2：统一个人搜索（已完成） | 第 2 周 | 能按客观字段和个人线索找歌 | 结果可解释，短关键词有兜底 |
| M3：播放适配验证（已完成） | 第 3 周 | 从结果唤起网易云或网页播放 | 失败可降级，不影响个人数据 |
| M4：导入与快速记录（已完成） | 第 4—5 周 | 导入收藏并记录正在播放歌曲 | 官方 CLI 真实收藏与快速记录均已验收 |
| M5：数据安全与自用（下一阶段） | 第 6 周起 | 备份恢复、真实使用和指标记录 | 20 个忘歌任务达到目标 |

## 5. 分阶段行动清单

### M0：整合 Cloud 数据层与工程基线

目标：把当前 Cloud 任务转化为本地可维护、可测试的正式代码。

#### 行动

- [x] 从 `main` 创建 `feature/music-data-layer` 分支；
- [x] 查看 Cloud diff，重点检查数据表、外键、删除策略、迁移顺序和 native 依赖配置；
- [x] 将 Cloud 任务应用到该分支；
- [x] 确认数据库只运行在 Electron 主进程，不暴露给渲染进程；
- [x] 确认包含以下实体或等价模型：
  - `tracks`；
  - `provider_tracks`；
  - `tags` 与 `track_tags`；
  - `notes`；
  - `memories` 与 `memory_tracks`；
  - `aliases`；
  - `sync_state`；
- [x] 检查平台歌曲失效时不会删除标签、感悟和事件；
- [x] 检查 pnpm `allowBuilds` 只批准必需的 native 构建包；
- [x] 补充或修正迁移、CRUD、关系约束和删除行为测试；
- [x] 建立 `AGENTS.md`，写明安装、检查、Cloud/Windows 边界；
- [x] 本地运行完整验证；
- [x] 提交并推送功能分支，经复核后合并至 `main`。

#### 验证命令

```powershell
pnpm install --frozen-lockfile
pnpm lint
pnpm format:check
pnpm typecheck
pnpm test
pnpm build
```

#### 完成标准

- 新数据库能从零迁移到最新版本；
- 重复启动迁移不会损坏数据；
- 核心 CRUD 与多对多关系测试通过；
- 删除平台映射不会删除个人记录；
- 工作树干净，分支已推送，CI 等价检查全部通过。

#### 验收记录（2026-07-15）

- 6 个数据库测试覆盖从零迁移、重复迁移、文件关闭/重开、核心 CRUD、多对多关系、约束和删除语义；
- `pnpm install --frozen-lockfile`、Lint、格式、类型、测试和生产构建全部通过；
- `pnpm package:dir` 成功为 Windows x64 打包并重建 `better-sqlite3`；
- 打包应用在隔离的 `--user-data-dir` 中启动并成功创建 `memory-music.sqlite3`；
- SQLite 模块仅由 `src/main` 和主进程测试引用，preload 与渲染进程未获得数据库访问能力。

### M1：歌曲资料与个人标注 CRUD

目标：不依赖网易云同步，先完成可用的个人资料管理闭环。

#### 行动

- [x] 定义主进程 IPC 服务和稳定 DTO，禁止渲染层直接访问 SQLite；
- [x] 实现手工添加歌曲：名称、歌手、专辑、网易云原始 ID 或链接可选；
- [x] 实现歌曲列表和歌曲详情页面；
- [x] 实现标签新增、复用、删除、合并和歌曲多选关联；
- [x] 实现多条带时间感悟，不使用覆盖式单备注；
- [x] 实现事件及其时间、地点、人物、描述；
- [x] 实现事件与多首歌曲的多对多关联；
- [x] 实现别名、错记歌词和其他召回线索；
- [x] 增加表单校验、空状态、保存失败提示和重试；
- [x] 为 IPC、Repository 和核心 Vue 交互增加测试。

#### 完成标准

- 能在 60 秒内手工添加一首歌并完成一次个人标注；
- 应用重启后全部数据仍存在；
- 同一标签可被多首歌复用；
- 同一事件可关联多首歌；
- 保存失败不会让界面误报成功或丢失用户输入。

#### 验收记录（2026-07-15）

- 单页操作路径支持“收录歌曲 → 选中详情 → 添加标签、感悟或召回线索”，无需等待平台同步；
- SQLite v2 迁移为事件增加地点和人物字段，v1 数据升级测试确认原记录不丢失；
- 7 个 Repository/迁移测试、4 个 IPC 服务测试和 4 个 Vue 交互测试共 15 项全部通过；
- 自动化验证同一标签可复用于多首歌、合并标签保持关系，同一事件可关联多首歌；
- Vue 交互测试确认保存失败时显示明确错误、不清空输入，只有确认成功后才重置表单；
- `pnpm lint`、格式检查、类型检查、测试、生产构建和 `pnpm package:dir` 全部通过；
- Windows 目录包在隔离用户目录中完成两次启动，SQLite `integrity_check` 为 `ok`，v2 数据在重启后仍存在。

### M2：统一个人搜索

目标：用真实个人线索找回歌曲，验证产品最核心的差异化价值。

#### 行动

- [x] 建立扁平化 `search_document` 或等价索引结构；
- [x] 索引歌名、歌手、专辑、别名、标签、感悟和事件文本；
- [x] 使用 SQLite FTS5 完成第一版全文搜索；
- [x] 规范化大小写、全角半角和常见空白符；
- [x] 评估简繁体、拼音全拼和首字母字段；
- [x] 为不足三个字符的关键词提供 `LIKE` 或内存模糊匹配兜底；
- [x] 搜索结果显示字段级命中原因；
- [x] 排序优先考虑个人字段命中、标题精确命中和最近编辑；
- [x] 建立 25 条可复现、贴近真实记忆场景的初始忘歌查询集，并明确 M5 用真实自用记录持续替换；
- [x] 记录无结果查询及缺失字段，不急于加入向量搜索。

#### 完成标准

- 标签、感悟、事件和别名均能命中；
- 结果明确显示“为什么匹配”；
- 搜索结果具有稳定排序；
- 20 条初始测试中至少 16 条能在 10 秒内找到目标；
- 索引重建不会修改原始个人数据。

#### 验收记录（2026-07-15）

- SQLite v3 新增 `search_documents`、FTS5 trigram 索引和无结果查询日志，v2 资料升级后歌曲、感悟和别名仍存在且可搜索；
- 搜索覆盖歌名、歌手、专辑、标签、感悟、事件、别名、错记歌词和其他线索，并在界面显示字段级原因；
- NFKC、小写和常见空白规范化通过测试，三字符以上走 FTS5，短查询使用参数化 `LIKE` 兜底；
- 排序固定考虑个人字段、标题精确、命中数量、最近编辑和歌曲 ID，重复查询顺序一致；
- 25 条初始忘歌查询全部命中预期首位并返回原因，整组自动化执行低于 10 秒门槛；
- 无结果查询保存在本地，用户可补充预期缺失字段；简繁体和拼音扩展已完成评估并延后到真实日志证明需要时；
- 索引重复重建前后，歌曲、标签、关系、感悟、事件和别名逐表对比完全一致；
- 8 个迁移/Repository 测试、6 个 IPC 测试、6 个搜索内核测试和 7 个 Vue 交互测试共 27 项全部通过；
- Lint、格式、类型、测试、生产构建和 Windows x64 目录打包全部通过；成品重启后 `integrity_check=ok`、数据库为 v3，原始歌曲与 FTS 命中均持续存在。

### M3：Windows 播放适配验证

目标：完成“找到歌曲后继续播放”的最短路径，不建设独立播放器。

#### 行动

- [x] 定义并固定 `PlaybackAdapter` 接口；
- [x] 建立 Mock 适配器供 Cloud 和自动化测试使用；
- [x] 在当前 Windows 电脑验证 `orpheus://` URL Scheme；
- [x] 用若干已知网易云原始歌曲 ID 测试唤起和播放行为；
- [x] 验证客户端版本变化风险、协议处理器拒绝和歌曲不可见等失败场景；
- [x] 实现打开网易云网页的兜底路径；
- [x] 验证 SMTC 能读取当前歌曲并控制播放、暂停、上一首和下一首；
- [x] 将协议唤起与 SMTC 分成两个独立适配器能力；
- [x] 记录测试过的网易云客户端版本和系统版本。

#### 完成标准

- 搜索结果可一键发起播放；
- 协议失败时能打开网页或给出明确提示；
- 播放失败不会影响数据库和个人记录；
- Cloud 测试不声称验证过 Windows 客户端；
- 不包含解锁音源、绕过会员或 DRM 的代码。

#### 完成证据（2026-07-15）

- 固定 `PlaybackAdapter` 与独立 `MediaSessionAdapter`；主进程校验不可信歌曲 ID 和平台映射，preload 只暴露类型化 `window.memoryMusic.playback` API；
- Windows 11 Pro x64 `10.0.26200`、网易云音乐 `3.1.36.205322` 真机验证 `orpheus://`；`347230`、`436514312`、`28815250` 均播放到预期歌曲，`185924` 真实显示“当前歌曲暂无音源”；
- 协议处理器拒绝会自动打开 `https://music.163.com/song?id=...`，双重失败返回稳定错误；客户端接受协议后仍在结果中保留手动网页兜底；
- 真实 SMTC 依次通过当前歌曲读取、暂停、播放、下一首和上一首；UTF-8/Base64 输出修复并回归了中文曲名；
- 播放失败测试逐表核对歌曲、平台映射、标签关系、感悟、事件、别名和搜索文档均不变化；
- 常规自动化 42 项通过，显式 Windows SMTC 集成运行 44 项通过；Lint、格式、类型、生产构建和 Windows x64 目录打包通过；
- 隔离成品完成“收录 → 搜索 → 真实唤起 → SMTC 刷新 → 应用内暂停 → 重启持久化”闭环，测试后删除隔离数据并恢复原媒体状态；
- 详细协议边界、命令、真实结果和升级回归清单见 [播放适配与 Windows 验证](播放适配与Windows验证.md)。

### M4：网易云数据导入与快速记录（已完成）

目标：降低建立个人资料库和持续标注的成本。

#### 行动

- [x] 申请并验证网易云开放平台所需凭据；
- [x] 在独立技术分支验证官方 `ncm-cli` 的登录、搜索和歌曲 ID；
- [x] 验证“我喜欢的音乐”的权限、分页、当前验收配额和收藏时间字段；
- [x] 实现 `NeteaseDataAdapter`，不让领域层依赖接口响应结构；
- [x] 首次导入只写入平台元数据，不覆盖个人字段；
- [x] 增量同步保存游标、时间、失败原因和重试状态；
- [x] 平台歌曲删除或不可见时只更新映射状态；
- [x] 建立正在播放歌曲的读取能力；
- [x] 增加全局快捷键和快速记录小窗；
- [x] 支持只输入一个标签或一句话即保存；
- [x] 未整理记录进入“待整理箱”。

#### 完成标准

- [x] 可导入至少 100 首真实收藏歌曲且不会产生明显重复；
- [x] 125 首 Mock 收藏重复同步幂等，不覆盖标签、感悟或事件；
- [x] 单次快速记录中位耗时不超过 10 秒；
- [x] Credential Manager 假凭据真机往返通过，凭据不进入日志、数据库明文或 Git；
- [x] 接口不可用时仍能离线搜索和编辑已有资料。

#### 阶段检查点记录（2026-07-19）

- SQLite v4 增加同步状态与 `quick_capture_inbox`；新文件迁移、v1/v2/v3 升级、重复迁移、文件关闭重开和映射删除保留个人数据均通过；
- 平台无关 Mock 以 3 页导入 125 首收藏，重复同步保持 125 首且不覆盖手工歌曲元数据、标签、感悟、事件或别名；失败后从最后成功页继续，不可见歌曲只更新映射；
- 不同网易云 ID 的同名同歌手条目保持独立，避免错误合并；未知适配器异常只持久化固定文案，不泄露可能的凭据；
- Windows Credential Manager 使用唯一虚假目标完成写入、读取和 `finally` 删除，验证后残留测试目标为 0；
- Windows 隔离成品从网易云窗口通过 `Ctrl+Shift+M` 唤起小窗，标签、一句话和零文本待整理均成功；5 次完整路径中位耗时为 1.733 秒；
- 小窗保存关闭后主窗口会在重新获得焦点时自动刷新，并直接显示新收录歌曲；
- 成品重启后记录仍可搜索，SQLite schema v4、`integrity_check=ok`；测试后删除隔离用户目录并保持原网易云歌曲和暂停状态；
- 当前常规测试 73 项通过、4 个环境项跳过；真实网易云 CLI 隔离验收单独通过，既有 Credential Manager 与 SMTC 真机证据继续保留；Lint、格式、类型、生产构建和 Windows x64 目录打包通过；详细证据见 [网易云导入与快速记录验证](网易云导入与快速记录验证.md)；
- 实名账号授权后重新读取动态命令树，确认红心入口为 `user favorite` + `playlist tracks`；`playlist collected` 是收藏歌单列表，不用于红心歌曲；
- 脱敏真实结构确认数字原始歌曲 ID、加密 ID、艺人、专辑、时长、`visible` 和 `extMap.addTime`；歌曲搜索也返回可用的原始 ID；
- 生产适配器固定官方 `@music163/ncm-cli@0.1.6`，每页 100 首，支持 offset 续传和完成后全量幂等刷新；不打包 CLI、不直连 OpenAPI、不复制 CLI 凭据、不记录原始输出；
- 临时新数据库完成至少 100 首真实收藏的首次全量导入、关闭重开和第二轮同步；没有新增重复歌曲，个人标签、感悟、别名和事件全部保留；
- M4 外部阻塞已解除并达到完成标准；公开 FAQ 未承诺固定每日额度，后续自用仍需监控配额和 CLI 动态契约变化。

### M5：数据安全、自用验证与 MVP 收口

目标：让产品能够长期自用，并用真实行为决定是否继续扩大投入。

#### 行动

- [ ] 实现数据库手动备份和自动轮换备份；
- [ ] 实现备份恢复，并用自动化测试验证；
- [ ] 实现 JSON 导出，后续再增加 Markdown 导出；
- [ ] 增加数据版本和导出格式版本；
- [ ] 连续自用至少 4 周；
- [ ] 记录 20 个真实忘歌任务和找回耗时；
- [ ] 记录标注耗时、无结果查询和失败播放次数；
- [ ] 每周清理一次标签重复、无效事件和错误关联；
- [ ] 根据数据决定是否加入查询语法、时间线或语义搜索；
- [ ] 形成 MVP 复盘和下一阶段决策。

#### 完成标准

- 备份可以在新数据库中完整恢复；
- 导出数据不依赖应用即可阅读或解析；
- 连续 4 周仍愿意主动记录；
- 忘歌测试达到 80%/10 秒目标；
- 已明确下一阶段是继续、缩减还是停止。

## 6. 优先级 Backlog

### P0：MVP 必须完成

- Cloud 数据层审查与整合；
- 数据库迁移、Repository 和自动化测试；
- 手工添加歌曲；
- 标签、感悟、事件和别名 CRUD；
- 统一全文搜索与命中解释；
- 网易云协议唤起或网页兜底；
- 备份、恢复和 JSON 导出；
- 真实忘歌测试集。

### P1：核心价值成立后加入

- 网易云喜欢列表同步；
- SMTC 当前歌曲读取和基础播控；
- 全局快捷键与快速记录；
- 待整理箱；
- 组合查询和保存搜索；
- 时间线与“去年的今天”。

### P2：明确延后

- 向量或大模型语义搜索；
- 多设备同步；
- 移动端；
- 多音乐平台；
- 社区、评论和好友；
- 独立完整播放器；
- 自动推荐系统。

## 7. Cloud 与本地开发分工

| 工作类型 | Codex Cloud | Windows 本地 |
| --- | --- | --- |
| TypeScript 领域模型 | 主要开发与测试 | 集成复核 |
| SQLite 迁移与 Repository | 主要开发与测试 | 文件与性能验证 |
| Vue 页面与普通交互 | 可开发和构建 | 最终体验验证 |
| FTS5 搜索与测试集 | 主要开发与测试 | 中文输入体验验证 |
| 文档、重构、静态检查 | 适合 | 复核 |
| `orpheus://` | 仅实现接口和 Mock | 必须真机验证 |
| Windows SMTC | 仅实现抽象和 Mock | 必须真机验证 |
| 网易云桌面客户端 | 不验证 | 必须真机验证 |
| Credential Manager | 不验证 | 必须真机验证 |
| Windows 安装包 | 可做配置检查 | 必须本地安装验证 |

### 7.1 本地交给 Cloud

```text
本地功能分支 → 完成检查 → commit → push → 从该远端分支启动新 Cloud 任务
```

任务提示应包含：

- 分支名和最新提交号；
- 本轮唯一主要目标；
- 允许和禁止修改的范围；
- 必须运行的验证命令；
- Windows 专属能力只能使用 Mock 的限制。

### 7.2 Cloud 结果接回本地

```text
查看 Cloud diff → 应用到干净功能分支 → 本地检查 → Windows 验证 → commit → push
```

不要把 Cloud 任务显示为 `READY` 等同于代码已经进入仓库；只有本地审查、验证、提交并推送后，才算进入产品基线。

## 8. 每个任务的完成定义

任何开发任务只有满足以下条件才能标记完成：

- 功能范围与任务描述一致，没有夹带无关重构；
- 新行为有自动化测试，或明确记录无法自动化的原因；
- 数据迁移和删除行为经过测试；
- 错误场景有用户可理解的反馈；
- 不在日志、截图、测试夹具和提交中泄露凭据；
- 通过以下检查：

```text
pnpm lint
pnpm format:check
pnpm typecheck
pnpm test
pnpm build
```

- Windows 专属功能完成真机验证；
- 相关 README、`AGENTS.md` 或设计说明已经更新；
- Git 工作树干净，提交信息能解释本次改动；
- 已推送远端，重要功能有可回退提交点。

## 9. 风险控制

| 风险 | 早期信号 | 行动 |
| --- | --- | --- |
| 标注成本过高 | 记录一次超过 10 秒、连续数日不记录 | 优先做快捷键、最近标签和一句话保存 |
| 搜索价值不足 | 真实忘歌测试仍需翻列表 | 补充缺失字段与命中解释，不先堆 AI |
| 数据模型过度设计 | 表很多但没有可操作界面 | 每个模型必须服务当前纵向闭环 |
| 网易云接口不稳定 | 同步频繁失败或字段变化 | 适配器隔离、离线可用、网页兜底 |
| Windows 集成误判 | Cloud 测试通过但真机失败 | Windows 功能单列验收清单和版本记录 |
| Native 依赖安装失败 | Cloud 或新机器无法 `pnpm install` | 锁定版本、限制 `allowBuilds`、验证干净安装 |
| 个人数据丢失 | 升级后记录缺失、恢复不可用 | 先备份再迁移，建立恢复测试 |
| 范围持续膨胀 | 同时开发推荐、社交、移动端 | 用 P0/P1/P2 队列和 MVP 指标约束 |

## 10. 近期行动：M4 完成后的下一有效开发周

### 已完成：数据基线、CRUD 与个人搜索（M0—M2）

1. [x] 审查、加固并合并 Cloud 数据层；
2. [x] 建立可重复迁移的 SQLite 数据库、Repository 和测试；
3. [x] 实现主进程 IPC 与手工添加歌曲；
4. [x] 完成歌曲列表、详情和全部个人标注 CRUD；
5. [x] 完成事件关系、召回线索、错误反馈和 Windows 成品冒烟；
6. [x] 建立统一搜索索引、字段级解释、短词兜底和无结果反馈；
7. [x] 建立并通过 25 条初始忘歌查询回归基线。

交付物：能够手工收录并标注歌曲，再用客观字段或个人记忆点找回目标并理解命中原因。

### 已完成：网易云数据导入与快速记录（M4）

1. [x] 取得有效开放平台凭据并完成 `ncm-cli` 扫码授权，核验账号权限、当前验收配额和收藏时间字段；
2. [x] 固定平台无关的 `NeteaseDataAdapter`，用 Mock 完成幂等导入、增量游标、失败重试和不可见映射测试；
3. [x] 首次导入与增量同步只维护平台元数据，不覆盖标签、感悟、事件、别名或其他个人字段；
4. [x] 复用已完成的 SMTC 当前歌曲读取，增加快速记录入口、全局快捷键和待整理箱；
5. [x] 在真实账号中导入至少 100 首收藏并完成分页、重复同步、关闭重开和离线退化验收。

本阶段交付物：可增量、可重试且不覆盖个人资料的收藏导入，以及中位记录耗时不超过 10 秒的快速记录路径。

### 下一步：数据安全与自用收口（M5）

1. [ ] 实现数据库手动备份与自动轮换备份；
2. [ ] 实现恢复到新数据库并覆盖成功、失败和版本不兼容测试；
3. [ ] 实现带格式版本的 JSON 导出；
4. [ ] 开始连续 4 周自用，记录 20 个真实忘歌任务、找回耗时、记录耗时和同步失败；
5. [ ] 以真实指标决定继续、缩减或停止后续功能。

## 11. 决策检查记录

M2 完成后进行第一次产品决策检查，判定规则如下：

- 如果个人线索明显提高找回率，继续投入播放适配和收藏同步；
- 如果搜索有效但记录成本过高，优先投入快速记录，而不是增加搜索算法；
- 如果记录成本可接受但找回率低，重新检查数据字段与搜索解释；
- 如果个人自用频率仍很低，暂停平台同步和复杂功能，重新评估痛点强度。

#### 检查结果（2026-07-15）

- 场景化基线中，标签、感悟、事件、别名和错记歌词均能把目标排到首位，个人字段已证明能提高可测试的找回率；
- 记录成本仍需真实连续自用观察，但不阻塞独立、可降级的播放适配验证；
- 下一步进入 M3，不提前进入收藏同步或语义搜索；真实自用数据不足仍作为 M5 的明确未完成项。

#### M3 检查结果（2026-07-15）

- 当前客户端中协议唤起对三首可用歌曲均成功，真实不可用歌曲有明确客户端反馈；协议处理器拒绝和双重失败由自动化稳定覆盖；
- 网页兜底既可在协议拒绝时自动执行，也可在客户端接受协议但未实际播放时由用户手动执行；
- 播放与个人数据写入完全隔离，失败路径的逐表快照无变化；
- 真实 SMTC 和成品闭环通过，M3 风险已降低到可接受范围；客户端升级仍需按版本清单回归；
- 决定进入 M4，但真实收藏导入必须以官方凭据、账号权限和 Windows 安全存储验收为前提，不使用非公开接口替代。

#### M4 检查结果（2026-07-19）

- 官方 CLI 动态契约、歌曲搜索、红心歌单、分页字段和收藏时间在实名登录态下完成脱敏核验；
- 至少 100 首真实收藏在隔离新数据库中完成两轮同步，重复同步无新增歌曲，个人标签、感悟、别名和事件均保留；
- Windows x64 目录成品在精简 `PATH` 下通过 `npx` 固定版本回退完成界面同步，落库歌曲数和网易云映射数与官方歌单声明数量一致，SQLite 完整性与成功检查点正常；
- 快速记录中位耗时 1.733 秒，收藏导入和记录成本均达到 M4 标准；
- M4 外部阻塞解除，决定进入 M5；CLI 版本、动态契约和每日配额继续作为外部依赖监控项。

下一次检查点设在 M5 完成首轮备份/恢复/导出后：先确认个人数据可以脱离应用安全恢复，再开始 4 周连续自用。

行动计划每完成一个里程碑更新一次，所有新增功能必须说明它提高了找回率、降低了记录成本、保护了个人数据，或降低了外部依赖风险中的哪一项。
